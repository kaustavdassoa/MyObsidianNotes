---
aliases:
  - Resume Optimizer Roadmap
tags:
  - project/resume-optimizer
  - planning
  - roadmap
  - backlog
created: 2026-04-04
---

# 🗺️ Resume Optimizer - TODO List

## 🚀 Features

* [x] **Generate detailed documentation** :  Generate detailed documentation. 
* [ ] **PDF Export Pipeline** #feature/export Add a PythonTaskNode using `pdfkit` or `WeasyPrint` to render the optimized HTML into a downloadable PDF.	
* [ ] **Create Streaming UI for the application** #UI: Create a reactJS UI 
	 -  User uploads Resume and Add JD 
	 -  Able to view events working in real time (Emit WebSockets or Server-Sent Events (SSE) from each Python task node to drive a real-time frontend progress bar.)
	 -  Able to view the report on completion of the pipeline. 
	 -  Able to download the resume.
- [ ] **Create a Docker Container** #Cloud-deploy:  
- [ ] **Human-in-the-Loop (HITL) Gate** #architecture/ux 
    - Pause the pipeline after the `[[Critic]]` node. Present the `changes_summary` to the user for approval before rendering the final HTML.

## 🚀 Optimization 

* [ ] **Make it work with other models** : Currently only working with Google geminin models - need to make it work with other models. 
* [ ] **Parallel Execution (Async Routing)** #architecture/performance 
    - *Logic:* Orchestrate the `[[DocumentParser]]` and `[[JDAnalyzer]]` to run concurrently instead of sequentially.
* [ ] **Multi-Model Routing** #ai/finops 
    - *Logic:* Route lightweight extraction tasks to smaller, faster models (e.g., Gemini 1.5 Flash-8B), reserving the heaviest models strictly for the rewrite phase.
- [ ] **Optimize session Management** #Context-Enginnering  ??
- [ ] **Refactor Folder structure** :
	- Move prompts into a separate folders
- [ ] **Testing Pipeline:**
	* Implement pyTest at each individual agent level.
	- Implement pyTest for end-to-end flow.
	- Work on agent evaluation - ??
- [ ] **Prompt Tunning** : 

