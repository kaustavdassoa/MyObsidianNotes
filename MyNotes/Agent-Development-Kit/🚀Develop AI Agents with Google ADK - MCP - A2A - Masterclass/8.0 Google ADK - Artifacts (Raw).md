
[Gemini Refer : https://google.github.io/adk-docs/artifacts/ 
Explain what is artifacts in Google ADK context, Why to use artifacts - Explain Core Concepts - `BaseArtifactService` `save_artifact`, `load_artifact`, or `list_artifacts` and `Delete Artifact` **Storage Options** available for artifacts - **In-Memory Artifact Service**
  (Temporary (lost when session ends or agent restarts)  **Persistent Storage (e.g., Google Cloud Storage - GCS)** (Durable, survives restarts)
]

[Gemini : Create a simple summarizer agent that summarize any text provided by the user and store the summary into a file, list the files in the artifact, read a file from artifact - to showcase how artifacts can be used in it create use case for `save_artifact`, `load_artifact`, or `list_artifacts` and `Delete Artifact`  ]


[Gemini : What is the limitation of **In-Memory Artifact Service** , How to persist the file using **Persistent Storage (e.g., Google Cloud Storage - GCS)** (Durable, survives restarts) - what is the benefit of using GCS Artifact service Refer : https://google.github.io/adk-docs/artifacts/#inmemoryartifactservice

Provide code snippet how to add a gcs_bucket while using adk web command 
Provide a code snippet to define a GcsArtifactService with a bucket name and attaching it to an agent. 
]